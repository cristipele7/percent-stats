services:
  db:
    image: postgres
    ports:
      - '5555:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: P3l3Cr1s71
      PGDATA: /var/lib/postgresql/data/pgdata
volumes:
  db-data:
